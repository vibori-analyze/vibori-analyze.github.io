# Vibori Pages

This repository publishes the generated site from [vibori-analyze/vibori](https://github.com/vibori-analyze/vibori) at <https://vibori-analyze.github.io/>.

The **Build and deploy** workflow checks the source, rebuilds its data index, generates the static application, and deploys the resulting artifact to GitHub Pages. It runs daily and can be started manually from the Actions tab.
